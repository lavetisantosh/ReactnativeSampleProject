import {GET_MOVIES} from './actions';

const initialState = {
    movies : []
}

function useReducers(state = initialState,actions){
  switch(actions.type){
      case GET_MOVIES  : 
      return 
  }
}